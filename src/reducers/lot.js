import * as ActionTypes from '../actions/actionTypes'

const initState = {
  test: ''
}

export default user = (state = initState, action) => {
  switch (action.type) {
    default:
      return state
  }
}